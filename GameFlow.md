# Echo Trace 游戏流程（按当前源代码同步）

本文档以当前仓库实现为准：

- **数值配置**：优先来自 `config/*.json`（分模块配置），并由服务端做安全边界 clamp；`game_config.json` 仅作为兼容回退。
- **协议**：JSON over WebSocket，路径为 `/ws`（详见 `protocol.json`）。

---

## 0. 客户端入口流程（状态机）

1. **CONNECT**
     - 输入 WebSocket 地址：`ws://<host>:<port>/ws`
     - 示例：`ws://127.0.0.1:8080/ws`（本地默认）或 `ws://139.224.0.73:9191/ws`（线上示例）
     - 可选：填写 **Resume ID（session_id）** 用于“冷启动续局”（只有填写才会尝试自动回到上次房间）
2. **LOGIN**
     - 输入“特工代号”（玩家名）
     - 客户端会把该名字用于创建/加入房间，并用于对局内登录包
3. **MENU**
     - CREATE：进入房间配置页
     - JOIN：进入房间列表并选择加入
4. **CONFIG（创建房间配置）**
     - 编辑配置表格（来自 `config/*.json` 合并后的模板；必要时回退到 `game_config.json`）
     - 创建房间：服务端会对超限值强制截断（客户端也会做同规则校正并提示）

---

## 1. 身份与房间规则（服务端权威）

- **session_id 派生规则**：由“特工代号（玩家名）”派生（例如 `agent_<name>`）
- **同名守护**：同一房间内特工代号必须唯一；重复会被拒绝（不会踢掉旧连接）

房间相关交互：

- LIST_ROOMS：拉取房间列表（含阶段/人数/地图尺寸等摘要）
- JOIN_ROOM：按 `room_id` 加入房间
- CREATE_ROOM：创建房间（房间名必须唯一）

---

## 2. 核心操作（默认键位）

### 2.1 基本操作

- `WASD`：移动
- 鼠标：决定面向（影响 90° 扇形视野方向）
- `Space`：开火（存在装填/冷却；客户端会做限频，服务端最终裁决）
- `E`：拾取/交互（部分旧机制仍可能保留，是否生效以当前实现为准）
- `F`：交互/商店（如果当前版本启用经济/商店模块）
- `ESC`：暂停菜单

### 2.2 道具/商店（可选模块）

本项目近期主轴是“坦克对战 + 迷雾 AOI”。如果某个版本仍启用背包/道具/商店：

- `1-6`：使用道具 /（商店打开时）购买
- `Shift+1-6`：丢弃
- `Ctrl+1-6`：出售（需在商人附近）
- `R`：刷新商品（规则与成本由服务端配置决定）

---

## 3. 视野与信息规则（核心体验）

- 视野为 **90° 扇形**，随面向变化
- 墙体会遮挡视线（LOS）
- 服务端会按“扇形 + LOS”做 AOI 过滤，只下发可见实体，避免信息泄露
- 客户端 Fog 表现与服务端规则对齐：扇形外黑屏、扇形内地图可见、实体仅在可见集合中出现

### 3.1 残影视野（近期新增）

- 为了提升对战手感与信息可追踪性，当前实现引入“残影视野”：目标离开视野/死亡后，可见信息会短暂保留（约 2 秒，具体以实现为准）

负重联动（配置已接入的部分）：

- `gameplay.weight_threshold_view_reduce`：超过阈值后视野半径缩小
- `gameplay.weight_threshold_noise_double`：超过阈值后移动噪音半径翻倍
- `gameplay.weight_threshold_immobilize`：达到阈值后无法进行关键交互（如破译/撤离）

---

## 4. 对局流程（AOI + 坦克对战主轴）

说明：项目仍保留“阶段/配置”的框架，但当前对局体验正向“坦克对战”收敛。本节用更贴近现状的方式描述流程。

### 4.1 大厅/战术选择（如启用）

- 加入房间后进入大厅阶段
- 玩家可用数字键选择战术倾向（以当前实现为准）：`1` RECON / `2` DEFENSE / `3` TRAP

### 4.2 进入战斗

- 玩家出生/复活到地图上，在迷雾 AOI 规则下进行对战
- 关键循环：移动 → 转向/瞄准 → 开火 → 利用墙体与反弹弹道进行对抗

### 4.3 弹丸与反弹（近期重构）

- 开火后生成弹丸（Projectile），在地图中飞行
- 弹丸命中墙体会发生反弹（Bounce）
- 命中目标会造成伤害（命中判定与伤害规则以服务端为准）

### 4.4 装填/开火节奏

- 存在服务端权威的装填/冷却（例如配置项 `combat.reload_time_sec`）
- 客户端会做基础限频，避免按键抖动导致的过量请求

---

## 5. 复活与观战（按当前实现）

- 死亡后会触发复活流程（Respawn），具体规则/延时由服务端控制
- 观战/自由镜头等表现如果启用，以客户端当前实现为准

---

## 6. 断线重连（当前实现）

- 服务端记录断线时间戳，在 `server.disconnect_grace_sec` 宽限期内允许同 `session_id` 重连恢复进度
- 超过宽限期会清除该玩家在该房间内的进度并视为离开
- 冷启动续局：CONNECT 页面填写 Resume ID（`session_id`）后，连接成功会自动尝试加入上次房间

